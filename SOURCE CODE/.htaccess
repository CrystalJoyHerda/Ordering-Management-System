RewriteEngine On

# Handle spaces in URL for SOURCE CODE directory
RewriteRule ^SOURCE\s+CODE(.*)$ "SOURCE%20CODE$1" [L]

# Forward requests to Employee directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ Employee/public/$1 [QSA,L]
